import styled from "styled-components";

export const Container = styled.form`
  
`;

export const SelftAttribuated = styled.label`
    cursor: pointer;
    border: 1px dashed var(--text-color);
    display: flex;
    align-items: center;
    padding: 12px 20px;
    border-radius: 5px;
    .label-control{
      margin-left: 10px;
      cursor: pointer;
    }
`;
